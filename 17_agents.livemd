# Elixir Koans - 17 Agents

```elixir
import ExUnit.Assertions
```

## Agents maintain state, so you can ask them about it

```elixir
{:ok, pid} = Agent.start_link(fn -> "Hi there" end)
assert Agent.get(pid, & &1) == ___
```

## Agents may also be named so that you don't have to keep the pid around

```elixir
Agent.start_link(fn -> "Why hello" end, name: AgentSmith)
assert Agent.get(AgentSmith, & &1) == ___
```

## Update to update the state

```elixir
Agent.start_link(fn -> "Hi there" end, name: :greeter)

Agent.update(:greeter, fn old ->
  String.upcase(old)
end)

assert Agent.get(:greeter, & &1) == ___
```

## Use get_and_update when you need to read and change a value in one go

```elixir
Agent.start_link(fn -> ["Milk"] end, name: :groceries)

old_list =
  Agent.get_and_update(:groceries, fn old ->
    {old, ["Bread" | old]}
  end)

assert old_list == ___
assert Agent.get(:groceries, & &1) == ___
```

## Somebody has to switch off the light at the end of the day

```elixir
{:ok, pid} = Agent.start_link(fn -> "Fin." end, name: :stoppable)

Agent.stop(:stoppable)

assert Process.alive?(pid) == ___
```

## Next Steps

* [Previous: Tasks](16_tasks.livemd)
* [Next: Genservers](18_genservers.livemd)
